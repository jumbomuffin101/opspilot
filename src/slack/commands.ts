import type { SlackHandlerRegistration } from "@/src/types/slack";

// Slash command registration is intentionally reserved for the next implementation stage.
export const commandRegistrations: readonly SlackHandlerRegistration[] = [];
